name: Audio8 Android APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Build temporarily paused while crash fix source is updated.'
